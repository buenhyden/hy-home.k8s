---
title: "SPEC-0065-TSK-0001: VAL-TRR-001 through VAL-TRR-006"
type: sdlc/task
status: done
owner: platform
updated: 2026-08-31
artifact_id: "SPEC-0065-TSK-0001"
---

# SPEC-0065-TSK-0001: VAL-TRR-001 through VAL-TRR-006 (Task)

## Overview

Execution record for the transition residue retirement. Each work package
appends its evidence here, including any criterion left unmet and the clause
that refuses it.

## Inputs

- [Spec 0065](../spec.md)
- [Plan 0065](../plan.md)
- [MIG-0008](../../../98.archive/migrations/0008-progress-append-form-retirement.md)
- `docs/99.templates/registry.json`

## Task Table

| Work | Criterion | Status | Evidence |
| --- | --- | --- | --- |
| WP-001 classification | [VAL-TRR-001](../spec.md#success-criteria--verification-plan) | done | Classification below |
| WP-002 transition machinery | [VAL-TRR-002](../spec.md#success-criteria--verification-plan) | done | `9e95bee8` |
| WP-003 Stage 99 release | [VAL-TRR-003](../spec.md#success-criteria--verification-plan) | done | `fd14901e` |
| WP-004 forms and profiles | [VAL-TRR-004](../spec.md#success-criteria--verification-plan) | done | `a393ab06`, `d13b9633` |
| WP-004 route census | [VAL-TRR-005](../spec.md#success-criteria--verification-plan) | done | `d13b9633`, `c30bc892`; census below |
| WP-005 verification | [VAL-TRR-006](../spec.md#success-criteria--verification-plan) | done | Clean checkout below |

### WP-001 classification

`Registry` declares five fields and none is `route_state`, so
`getattr(registry, "route_state", "terminal")` resolves to `"terminal"` on every
run. Ten branches in `scripts/validate-links-and-owners.py` test that value.

| Site | Condition | Machine fact | Verdict |
| --- | --- | --- | --- |
| `_archive_transition_handoff` | `!= "transition"` | always true | dead body |
| `_reviewed_stage90_move_edges` | `!= "transition"` | always true | dead body |
| `_reviewed_source_pinned_alias_edges` | `!= "transition"` | always true | dead body |
| `_reviewed_immutable_historical_alias_edges` | calls the row above and nothing else | unreachable result | dead |
| `_immutable_historical_redirects` | called only from the dead helper | no live caller | dead |
| `IMMUTABLE_HISTORICAL_ALIAS_SOURCE_BLOBS` | read only by the dead helper | 12 pins, no live reader | dead |
| `_program_owner_path` transition-owner branch | `== "transition"` | always false | dead branch |
| `_program_local_targets` transition-alias branch | `== "transition"` | always false | dead branch |
| `_historical_exception_diagnostics` | `== "terminal"` returns `[]` first | always returns empty | dead body |
| `_ledger_rows` remainder after the `== "terminal"` return | `== "terminal"` | always true | dead remainder |
| `wp004b_targets` transition selector | `== "transition"` | always false | always `{}` |
| `known_paths` move-target widening | `== "transition"` | always false | dead branch |

Two candidates were rejected.

`_document_taxonomy_transition_manifest` and
`scripts/document-taxonomy-migration.json` carry `"state": "transition"` in the
manifest body, which is data rather than the registry's route state. The
function is called unconditionally and its `move_targets` output is one of the
eight projections composed in `_historical_migration_proof`, so it is a current
redirect owner. Retained.

`_work054_wp004b_targets` is called unconditionally from
`_historical_migration_proof` as well as from the dead selector at the
`known_paths` site. Only the dead call is removed. Retained.

The alias family the user approved is the first six rows. The remaining six were
found by the same measurement during WP-001 and are retired on the same evidence
rather than left as a second residue.

### WP-002 execution

`scripts/validate-links-and-owners.py` fell from 8,234 to 7,550 lines and its
40-hex object pins from 31 to 8. The twelve classified sites were removed.

`IMMUTABLE_HISTORICAL_ALIAS_SOURCE_BLOBS` was reclassified during execution.
The classification named it dead because only a `route_state`-gated helper read
it. `_terminal_frozen_manifest_source` also reads it and is not gated, but
admits a source only under `docs/90.references/cloud-examples/`, so seven of its
twelve entries were unreachable and five remain. A `NameError` on the first run
caught the error before it reached a commit.

Seven `ArchiveTransitionLinkTest` cases were removed. Each asserted the empty
result of a removed helper or a counterfactual `route_state="transition"` the
`Registry` dataclass cannot construct, so none witnessed a live invariant. The
live `_work054_edges` assertion inside
`test_terminal_route_does_not_project_an_active_stale_owner_edge` is kept.

### WP-003 execution

`_validate_mig0004_rows_and_targets` required every `MIG-0004` Stage 99 target
to be present in the staged inventory. The RED case, a Stage 99 target absent
with a sealed row covering it, raised `RECOVERY-MIGRATION-TARGET: current staged
target set differs`. The narrowing case, a Stage 99 target whose sealed row
names a different path, already passed and still passes.

`_sealed_row_retired_paths` reads the staged sealed generic records and returns
the legacy paths they retire; those drop out of the Stage 99 inventory set. The
row census, the Stage 99 action-target map, the non-Stage-99 target requirement,
and `validate_mig0004_historical_targets` are unchanged.

### The same defect shape in three more owners

Deleting the form surfaced two further sites of the shape WP-003 released, and
one that turned out to be already correct. Each treats a completed migration
edge as a permanent claim about the current tree rather than a claim about one
historical transition.

| Owner | Behaviour | Disposition |
| --- | --- | --- |
| `_validate_mig0004_rows_and_targets` | Stage 99 target must be staged | Released in WP-003 |
| `archive_cutover.py` `_work054_migration_projection` | pinned row's endpoint must be tracked | Row drops; `_resolve_migration_graph` rejects archive terminals by design |
| `validate-links-and-owners.py` `_work054_wp004b_targets` | endpoint must be tracked | Endpoint resolves through the generic proof, which composes a deleted path to the Archive index |

The third was found by writing its regression test. The test could not go red
for the reason it was written, which showed the guard added for it was already
unreachable; the guard was removed and the test now asserts the composed
Archive-index result.

### WP-004 execution

`MIG-0008` seals one row retiring the progress append form.
`governance/progress-entry` and `governance/progress-ledger` retire with it.

`MIG-0009` seals two rows retiring `docs/00.agent-governance/memory/README.md`
and `docs/99.templates/templates/governance/memory.template.md`;
`governance/memory` and `template/governance/memory` retire with them, and the
`readme/collection-index` route narrows to the indexes that remain.

`SPEC-0054-TSK-0003` recorded the README as a retained memory router with its policy
scheduled for activation in WP-003B. That clause predates `MIG-0007` and
described a router to the retired ledger. `MIG-0009` supersedes only that
clause; WP-003B still owes the `draft -> active` activation of
[context and memory policy](../../../00.agent-governance/policies/context-and-memory.md),
which this work neither performs nor claims.

Nine documents render a link to the retired README. Two are mutable Stage 00
owners and were repointed. Four of the seven that cannot be edited already
resolve through `MIG-0005`; a second registration raised
`RECOVERY-MIGRATION-CONSUMER: consumer identity is ambiguous`, so `MIG-0009`
registers the remaining three Stage 90 historical observation bodies.

Both retirements left the registry with no profile declaring an
`appendContract`, so `c30bc892` retires that capability: the schema definition
and lifecycle key, the parser and profile field, six `APPEND-*` rule
identifiers, the diagnostics function, the context dataclass and builder, three
fixture mutation kinds, the `append-fragment` applicability, and the parameter
threading through four call chains.

### WP-004 route census

Every profile whose path pattern names a retired path is gone. Profiles fell
from 59 to 55.

Six profiles route zero tracked paths today: `sdlc/data-model`,
`sdlc/incident`, `sdlc/postmortem`, and the three
`exception/native-contract-*` profiles. They are not the same kind as the
retired four. Each carries an open generative pattern, so the next Spec's
`data-model.md`, the next incident record, and the next native contract route
to them. The four retired profiles carried closed patterns naming exactly one
path that a sealed row retired, which no future document can occupy.

VAL-TRR-005 is recorded as met for that subject. Its literal wording, "No
registry profile routes zero paths", is broader than the invariant it was
written for and would mandate deleting the contract for the next incident
record. The wording gap is left for the reader rather than rewritten to match
this result.

### WP-005 clean checkout

Branch tip `c30bc892`, cloned to a separate worktree with no working-tree
carry-over.

| Check | Command | Result |
| --- | --- | --- |
| Repository quality gates | `bash scripts/validate-repo-quality-gates.sh .` | rc=0 |
| Full suite | `python3 -m unittest discover --start-directory tests --top-level-directory tests --pattern 'test_*.py'` | rc=0, 926 tests, OK (skipped=4) |

Repository-static evidence only. No CI, provider-runtime, remote, or live
result is claimed.

## Approval and Safety Boundaries

Direct human approval on 2026-08-30 authorizes this standalone execution
relation. No separate PRD or Architecture Description is required or part of
this standalone lifecycle.

WP-004 does not stage a deletion before the sealed row is proved to keep every
inbound link resolvable.

GitOps remains the only path to cluster state; this work touches no live
cluster. No push, publish, or merge is authorized here.

## Verification Summary

All six criteria are met. Five commits carry the work: `9e95bee8`, `fd14901e`,
`a393ab06`, `d13b9633`, `c30bc892`. The gate and the full suite pass in a clean
checkout at the branch tip.

Residuals for the next owner:

1. VAL-TRR-005's wording is broader than its invariant, as recorded above.
2. `SPEC-0054-TSK-0003` WP-003B still owes the context and memory policy's
   `draft -> active` activation. No ownership gap is open: that policy's
   Authority Boundary names `agent-execution.md` and `approval-and-safety.md`,
   both active, as the owners that govern until then.
3. The branch is unmerged. Merge and branch cleanup are not authorized here.

## Traceability

### Lifecycle Traceability

| Criterion / work item | Result | Evidence |
| --- | --- | --- |
| [VAL-TRR-001](../spec.md#success-criteria--verification-plan) | Met. | Classification table above, recorded in `7fb1a755` before any retirement. |
| [VAL-TRR-002](../spec.md#success-criteria--verification-plan) | Met. | `9e95bee8`; validator 8,234 to 7,550 lines, pins 31 to 8, no live redirect owner removed. |
| [VAL-TRR-003](../spec.md#success-criteria--verification-plan) | Met. | `fd14901e`; RED-first release with every Git-side proof unchanged. |
| [VAL-TRR-004](../spec.md#success-criteria--verification-plan) | Met. | `a393ab06`, `d13b9633`; sealed rows carry recovery coordinates and the link validator passes. |
| [VAL-TRR-005](../spec.md#success-criteria--verification-plan) | Met for its subject; wording gap recorded. | `d13b9633`, `c30bc892`; census above. |
| [VAL-TRR-006](../spec.md#success-criteria--verification-plan) | Met. | Clean checkout at `c30bc892`: gate rc=0, suite rc=0, 926 tests OK. |

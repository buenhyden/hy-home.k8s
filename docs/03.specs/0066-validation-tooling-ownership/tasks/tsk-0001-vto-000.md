---
title: 'Task: Validation Tooling Ownership Restructure'
type: sdlc/task
status: in-progress
owner: platform
updated: 2026-09-01
artifact_id: "TSK-0066-0001"
---

# Task: Validation Tooling Ownership Restructure

## Overview

Execute PLAN-0066 WP-001 through WP-012 as the delegated implementation of
Spec 0054 WP-010 and WP-011. Spec 0054 remains the integrated acceptance
owner. This Task is `in-progress`; its parent-owned activation is established,
WORK-001 through WORK-003 are complete, and WORK-004 is the current execution
boundary.

The work moves and reuses the existing validation routing contract, corrects
current executable-reference and staged-index behavior, replaces production
self-tests with independent top-level tests, removes production fixture
dependencies, gives each rule one semantic owner, thins runners, simplifies
routing deliberately, and retires only wrappers and pins with sufficient
consumer and recovery evidence.

## Inputs

- [Plan](../plan.md)
- [Spec](../spec.md)
- [Current Spec Index](../../README.md#current-spec-index)
- Parent acceptance identifiers: `SPEC-0054`, `WP-010`, `WP-011`,
  `TSK-0054-0010`, and `TSK-0054-0011`. Their rendered parent relation is
  owned by this package's Spec, not by a cross-package Plan/Task link.
- [Accepted ADR-0031 — current corpus retention and validation ownership](../../../02.architecture/decisions/0031-current-corpus-retention-and-validation-ownership.md)
- [ADR-0030 — authority-first SDLC and agent governance convergence](../../../02.architecture/decisions/0030-authority-first-sdlc-and-agent-governance-convergence.md)

## Task Table

| ID | Upstream criteria | Work item | Owner | Status | Result | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| WORK-001 | VAL-VTO-001 | Verify the parent-owned activation transaction and capture the validation-tooling baseline; do not repeat its ADR, README, lifecycle, compatibility-pointer, or delegated-state transitions | platform | Done | Parent activation, delegated ownership, current routing selection, production self-test/fixture dependencies, duplicate invocation surfaces, and pin candidates captured without mutating tooling. | Accepted ADR/README reciprocity; closed package-local component; no child standalone row; `942de1d2`; focused ownership tests; registry summary and representative selection below |
| WORK-002 | VAL-VTO-004 | Correct current executable-reference and sealed-history distinction | platform | Done | The aggregate delegates one extension-neutral, depth-aware rule owner. Current sources require tracked regular files; Stage 03 proposals claim no current path; terminal and Stage 90 evidence use bounded reachable-Git recovery; Stage 98 remains with the Archive owner. | Initial module-import RED; 8 focused GREEN cases; actual tracked corpus diagnostics=0; strict registry/Markdown/link/lifecycle and aggregate repository quality PASS |
| WORK-003 | VAL-VTO-002, VAL-VTO-003 | Atomically move and reuse the validation registry JSON/schema and update consumers | platform | Done | The JSON/schema pair exists only under `scripts/validation/`; executable consumers, tests, current architecture, current Plans, and current links use that owner. | Git recognizes two atomic renames; representative affected selection is canonical-JSON identical; focused and strict gates below PASS |
| WORK-004 | VAL-VTO-005, VAL-VTO-006, VAL-VTO-008 | Establish independent behavior coverage for ownership, tests, I/O, subprocess, and staged-index rules | platform | In Progress | WORK-003 is green; independent coverage audit is the current boundary. | Focused RED/GREEN behavior evidence without a fixed case count |
| WORK-005 | VAL-VTO-005, VAL-VTO-009 | Record the current ownership/disposition in this Task; remove dead code and classify rules and pins | platform | Queued | Not executed | Task-local table and current diff |
| WORK-006 | VAL-VTO-006 | Remove production self-tests and dependencies on top-level tests/fixtures | platform | Queued | Not executed | Independent suite; production import/read sweep |
| WORK-007 | VAL-VTO-005, VAL-VTO-008 | Consolidate or split by responsibility and promote shared bounded primitives | platform | Queued | Not executed | One semantic owner per rule; bounded-I/O tests |
| WORK-008 | VAL-VTO-005 | Reduce aggregates and runners to dispatch and result normalization | platform | Queued | Not executed | Rule-logic scan and focused behavior parity |
| WORK-009 | VAL-VTO-007 | Retire only wrappers and aliases with consumer-zero and unique-diagnostic-zero proof | platform | Queued | Not executed | Per-candidate consumer, diagnostic, and recovery evidence |
| WORK-010 | VAL-VTO-003, VAL-VTO-010 | Simplify selection, hooks, and CI routing while preserving required external check names | platform | Queued | Not executed | Before/after lane evidence and duplicate-invocation audit |
| WORK-011 | VAL-VTO-009 | Retire current-state SHA/digest tracking and document retained immutable pins | platform | Queued | Not executed | Pin owner and update/recovery classification |
| WORK-012 | VAL-VTO-011 | Complete and commit all implementation, review, and acceptance-bearing gates; obtain TSK-0054-0011 parent acceptance; then perform only the state closure and post-state lifecycle/diff confirmation | platform | Queued | Not executed | Committed clean-checkout evidence before acceptance, parent acceptance, `TSK-0066-0001 in-progress → done`, Plan/Spec `active → done`, and post-state lifecycle/diff PASS |

### Point-in-Time Baseline

The clean activation commit `942de1d2` is the comparison point for this Task,
not a mutable branch pin or permanent count contract. The baseline commands
were read-only and returned:

| Concern | Observed baseline | Disposition boundary |
| --- | --- | --- |
| Routing owner | schema v2 at `docs/00.agent-governance/contracts/validation-surfaces.json`; 23 validator records, 4 CI jobs, 21 surfaces | Move this exact JSON/schema pair atomically to `scripts/validation/registry.*`; do not copy it or preserve the counts as policy. |
| Representative affected selection | six paths spanning the current registry, validator, test, workflow, Spec, and GitOps surfaces selected 22 validators and all 4 CI jobs with no unmatched path at protected level | Re-run the same path set immediately after the move and require identical JSON selection only for that move checkpoint. |
| Repository file surface | 45 tracked files under `scripts/`, 54 under `tests/`, and 20 under `tests/fixtures/` | Counts are inventory evidence only; responsibility and current consumers decide retention. |
| Embedded self-test surface | 17 production Python entrypoints expose `--self-test`; workflow, pre-commit, aggregate, and manual wrappers invoke overlapping subsets | Establish independent top-level coverage, then remove production self-test branches and duplicate invocations rather than fixing a target count. |
| Production test-data coupling | affected-surface, Markdown, agent checkpoint/governance/loop/provider, GitOps change-set, link-owner, and legacy-cutover validators name `tests/` or `tests/fixtures/` paths | Classify each as self-test-only, production runtime data, or dead compatibility; production modules may retain none of these reads. |
| Aggregate and wrapper overlap | `validate-repo-quality-gates.sh`, `validate-affected-surfaces.py`, `run-validation-lane.py`, pre-commit, CI, and `validate-harness.sh` overlap in routing or invocation | Keep one routing graph; make aggregates dispatch/normalize only; retire a wrapper only after consumer-zero and unique-diagnostic-zero proof. |
| SHA/digest candidates | archive/recovery, document-contract/lifecycle/link, migration, agent-governance CI, and CI Python modules contain pinned commit/blob/document/script identities | Retain only external supply-chain or sealed recovery byte identities with an owner; remove current document/script/branch pins and transition-only snapshots. |

Baseline verification also passed the 8 delegated-ownership cases, 45 strict
document-cutover cases, strict cross-document validation, staged lifecycle
validation, 112 Archive/Recovery regressions, aggregate repository quality,
and staged pre-commit. These results establish a mutation starting point; they
do not prove the later work packages complete.

### WORK-002 Verification

The first focused run failed because the independent
`scripts/validation/current_executable_references.py` owner did not exist. The
implemented owner then passed 8 cases covering nested non-shell extensions,
present and missing current targets, a current target that also exists in Git
history, non-terminal Stage 03 proposals, Git-recoverable and unrecoverable
terminal references, Stage 98 delegation, and registry-derived suffixes.

The staged repository scan returned zero executable-reference diagnostics.
Strict registry and Markdown validation, strict cross-document validation,
staged lifecycle validation, Python compilation, diff checks, and the complete
repository quality aggregate passed. The aggregate no longer embeds either
legacy `.sh`-only scan and does not read `tests/fixtures/` or Stage 98 payloads
for this rule.

### WORK-003 Verification

Git recognizes the former Stage 00 JSON and schema as atomic renames to
`scripts/validation/registry.json` and
`scripts/validation/registry.schema.json`; neither source path remains and no
second routing owner was introduced. The registry's relative `$schema` value
and schema const now name `./registry.schema.json`, while the schema `$id`
remains stable. Current execution consumers, focused tests, Stage 00 quality
policy, current Stage 02 architecture, active/draft Stage 03 guidance, and the
latest research pack's Related Documents link use the target path.
Point-in-time rows in completed SDLC artifacts and preserved research
observations retain their historical path evidence.

The same six logical paths used at baseline—substituting only the moved
registry target for its source—produce canonical-JSON-identical `affected`
selection: the same validators, four CI jobs, `protected` level, and no
unmatched paths. The target belongs to the `scripts` surface, so that surface
now includes its previously omitted `agent-roster-currentness` owner; its two
existing fixture expectations were minimally aligned and remain scheduled for
independent-test replacement under WORK-006.

The affected-surface self-test and production validation passed, with the
production scan reporting every tracked path covered exactly once. The 16
migration regressions, 118 consumer/current-executable-reference tests,
agent-governance CI contract, strict document registry, and strict Markdown
validation passed. These are repository-static results and do not claim hosted
CI, branch protection, provider runtime, or live-system execution.

## Approval and Safety Boundaries

- **Allowed Paths**: `scripts/**`, `tests/**`, the existing
  `docs/00.agent-governance/contracts/validation-surfaces.{json,schema.json}`
  only as the source of the atomic move, `docs/03.specs/0066-validation-tooling-ownership/**`,
  validation-related `.github/workflows/**`, `.pre-commit-config.yaml`, and
  current validation hook documentation. The parent-owned activation
  transaction is complete before these execution paths become effective.
- **Forbidden Paths**: unrelated user changes; sealed Stage 98 payload edits;
  Spec 0054 work packages outside WP-010/WP-011; live cluster, credentials,
  provider-runtime, release, push, merge, and deployment surfaces.
- **Activation Completed**: TSK-0054-0010 owned the exact activation index.
  ADR-0031 is accepted with reciprocal predecessor relations, ADR-0030 carries
  only the scoped amendment, the Stage 02/03 projections are aligned, and the
  package-local delegated-ownership gate and focused cases are green. In the
  same transaction TSK-0054-0010 became `done`, this Task became
  `in-progress`, TSK-0054-0011 became the sole `in-progress` parent acceptance
  Task, and the parent compatibility pointer moved to TSK-0054-0011. No Spec
  0066 standalone row or Stage 99 lifecycle-domain/schema/projection change was
  created.
- **Additional Approval Required**: remote branch-protection inspection or
  mutation, required check-name changes, push, merge, live operations, or scope
  beyond the delegated work packages.
- **Static Validation**: focused independent tests, affected and staged lanes,
  registry/Markdown/cross-link/lifecycle gates, aggregate repository quality,
  and worktree/index diff checks.
- **Live Validation**: `DEFER` — repository-static evidence proves no hosted,
  provider-runtime, or cluster behavior.
- **Secret / Vault Handling**: no secret or credential value is read or printed.
  Existing secret-scan allowlist markers are changed only when their owning pin
  is explicitly classified.
- **Rollback Plan**: per logical commit; preserve the worktree, revert only the
  failed logical unit when authorized, and never edit a sealed Migration as
  rollback.
- **Evidence Location**: this Task for point-in-time implementation evidence;
  reachable Git for execution and recovery history; a sealed Migration only
  for a required immutable lookup Git alone cannot resolve.

## Verification Summary

WORK-001 through WORK-003 are complete. The delegated component is active, its
point-in-time baseline is recorded, current executable-reference ownership is
independently tested and aggregate-integrated, and the routing registry now has
one executable owner. WORK-004 is in progress; later implementation work
remains pending. Each completed work
item records exact commands, result, relevant paths, and limitations.

All evidence in this Task is repository-static. It does not prove native agent
discovery, hook delivery, authenticated provider runtime, hosted CI, branch
protection, or live cluster readiness.

## Traceability

### Lifecycle Traceability

| Criterion / work item | Result | Evidence |
| --- | --- | --- |
| [VAL-VTO-001](../spec.md#success-criteria--verification-plan) | Done. | Parent activation, Spec 0054 relation, focused delegated ownership, current routing selection, dependency baseline, and broad repository-static validation recorded above |
| [VAL-VTO-002](../spec.md#success-criteria--verification-plan) | Done. | Atomic Git renames, source absence, target-only consumers, stable schema identity, and no duplicate registry |
| [VAL-VTO-003](../spec.md#success-criteria--verification-plan) | Done for the move checkpoint. | Canonical-JSON-identical affected selection for the six representative paths; later routing simplification remains WORK-010 |
| [VAL-VTO-004](../spec.md#success-criteria--verification-plan) | Done. | 8 focused current/proposal/history/sealed cases, zero actual-corpus diagnostics, and aggregate repository quality PASS |
| [VAL-VTO-005](../spec.md#success-criteria--verification-plan) | Not executed. | Pending rule-owner and thin-runner audit |
| [VAL-VTO-006](../spec.md#success-criteria--verification-plan) | Not executed. | Pending independent tests and production dependency sweep |
| [VAL-VTO-007](../spec.md#success-criteria--verification-plan) | Not executed. | Pending wrapper retirement evidence |
| [VAL-VTO-008](../spec.md#success-criteria--verification-plan) | Not executed. | Pending bounded-I/O and staged-index cases |
| [VAL-VTO-009](../spec.md#success-criteria--verification-plan) | Not executed. | Pending pin classification |
| [VAL-VTO-010](../spec.md#success-criteria--verification-plan) | Not executed. | Pending CI compatibility evidence |
| [VAL-VTO-011](../spec.md#success-criteria--verification-plan) | Not executed. | Pending clean-checkout closure |

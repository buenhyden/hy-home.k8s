# Target: docs/03.specs/<feature-id>/contracts/schema.graphql
# Owner docs from target directory:
# - Parent spec: ../spec.md
# - API spec: ../api-spec.md
schema {
  query: Query
}

type Query {
  example(id: ID!): Example!
}

type Example {
  id: ID!
  status: String!
}

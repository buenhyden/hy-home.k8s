kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker
# NOTE: Adjust node images and resource settings for your environment.
# This config is a template used in README quick-setup and workflow examples.

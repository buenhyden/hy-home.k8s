#!/usr/bin/env bash
# pre-commit — thin entry for the chained Git hook runner.
# Git resolves one hook directory, so this entry names its hook and
# forwards the payload; the chain itself is owned by chained-hook.sh.
set -euo pipefail

exec bash "$(dirname "$0")/chained-hook.sh" pre-commit "$@"

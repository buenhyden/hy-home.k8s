apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: opensearch-external-se
  namespace: default
spec:
  hosts:
    - opensearch-external.default.svc.cluster.local
  addresses:
    - 172.19.0.45
  ports:
    - number: 9200
      name: http-opensearch
      protocol: TCP
  location: MESH_EXTERNAL
  resolution: STATIC

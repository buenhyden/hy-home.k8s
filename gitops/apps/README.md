# Apps (Workloads)

Workload applications (Deployments/Services/Ingress/etc.) live here and are intended to be pulled into `gitops/clusters/local/90-apps` via Kustomize.

For v1, this directory is intentionally minimal.
